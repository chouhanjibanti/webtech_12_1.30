String :- String is a combination of characters.

we can write the String in the 3 ways :- 
1. ''
2. ""
3. `` -> template literals -> multi line string , varibale display

let s = "debugshala"



Recursive function :- 
A recursive function in js is a function that calls itself until a certain condition met.

function funName(){
    if(condition){
        return value;// base case - condition
    }

    return funName();/ recursive call 
}

=================


Advanced Question of an Array :- 


1.  Remove the duplicate element from the array and store in the new Array.
2.  Sort the Array in ascending order.[Bubble sort]
3.  Sort the array using the selection sort.
4.  We can find the largest element from the array .
5.  find the occurrence of each element from the Array.