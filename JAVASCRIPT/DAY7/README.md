Array :- Array is a non primitive datatype.

Array is huge memory block whos store the diff types of element/values.
Heterogenous :- diff types of element.
Homo :- Same types of element.

Array Indexes start from the 0.
total length -> count
let arr = [1,3,3,'hy','jay',true]
index - 5
length -> number of elemment -> 6

if you want to extract the element thrugh the index.
arr[index]



Methods of Array :- 
1. push 
2. pop 
3. shift 
4. unshift 
5. indexOf
5. slice 
6. splice
7. include
8. find 
9. findIndex
10. some 
11. Every 


Iteration Method 
1. forEach 
2. for of 
3. for in 
4. map 
5. filter 
6. reduce
// 8. find 
// 9. findIndex
// 10. some 
// 11. Every


Array Questions :- 

1. 
let arr = [10, 20, 30, 40];
Remove the first element using print the updated array.

2. 
let arr = ["Apple", "Banana", "Mango"];
Remove the first element, store it in a variable, and print both the removed value and the updated array.


3. 
let roles = ["User", "Guest"];
Add "Admin" at the beginning.

4. 
let nums = [3, 4, 5];
Add 1 and 2 at the start of the array and print the new array and its length.


5. 
let numbers = [2, 4, 6, 8];
Create a new array where each value is multiplied by 2 using map().

6. 
let names = ["john", "doe", "alice"];
Convert all names into uppercase using map().


5. 
let prices = [100, 200, 300];
Find the total sum using reduce().

6. 
let marks = [45, 67, 89, 32, 76];
Find the highest mark using reduce().


7. 
let nums = [10, 15, 20, 25, 30];
Filter out only the even numbers.

8. 
let ages = [12, 18, 25, 16, 30];
Filter ages that are 18 or above.


9. 
let colors = ["Red", "Green", "Blue", "Yellow"];
Remove 2 elements starting from index 1 using splice().

10. 
let courses = ["HTML", "CSS", "Node"];
Insert "JavaScript" and "React" at index 1 using splice().


11. 
let arr = [5, 10, 15, 20, 25, 30];
Create a new array from index 1 to 4 using slice().

12. 
let arr = ["A", "B", "C", "D", "E"];
Copy the last 3 elements using slice().


13. 
let tech = ["HTML", "CSS", "JavaScript", "React"];
Find the index of "React".

14. 
let nums = [11, 22, 33, 44];
Check whether 33 exists in the array using indexOf().